<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:padding="16dp"
    android:background="@color/cent"
    android:layout_height="wrap_content">

    <TextView
        android:background="?android:attr/selectableItemBackground"
        android:gravity="center_vertical"
        android:drawableEnd="@drawable/ic_drop_down"
        android:text="@string/change_in_list"
        android:textColor="@color/light_gray"
        android:id="@+id/listPickButton"
        android:padding="16dp"
        android:drawablePadding="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="1px"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:background="@color/color_background_info_card"
        android:visibility="visible">

    </FrameLayout>
    <TextView
        android:background="?android:attr/selectableItemBackground"
        android:gravity="center_vertical"
        android:drawableEnd="@drawable/ic_add_not_ring"
        android:text="@string/add_in_list"
        android:textColor="@color/light_gray"
        android:id="@+id/addInListPickButton"
        android:padding="16dp"
        android:drawablePadding="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

</LinearLayout>